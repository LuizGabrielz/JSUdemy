//Tipos de dados primitivos - String, number, undefined, null, boolean e symbol(ainda vamos ver)
//Podemos criar uma String com aspas simples, duplas e com crase(template string)

const nome ='Luiz';//String
const nome1 = "Luiz";//String
const nome2 = `Luiz`;//String

//Podemos criar tambem um Number 
const num1 = 10; //Number.
const num2 = 10.52; // Também pode considerar um Number.

let nomeAluno; // Quando eu faço isso, essa variável automaticamente recebe o valor deundefined.
// Não aponta para lugar nenhum na memória.

let sobrenomeAluno = null; 
//Esse tipo de valor significa NULO e não aponta para lugar nenhum da memória.
//Mas eles não são iguais, nós utilizamos o null quando queremos colocar o valor como nulo.

const boolean = True; //Verdadeiro ou Falso.

console.log(typeof nome); //Saber o tipo de dados.
console.log(typeof nome, nome); //Saber o tipo de dados e o valor.

 